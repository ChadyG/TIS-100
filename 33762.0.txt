@0
START:
JGZ ELONE
 SUB UP
 JLZ SUCCESS
 MOV 0, ACC
 MOV 0, DOWN
 JMP START
ELONE:
 MOV 0, DOWN
 MOV UP, ACC
 JMP START
SUCCESS:
  MOV 1, DOWN
  MOV 1, ACC

@1
START:
JGZ ELONE
 SUB UP
 JLZ SUCCESS
 MOV 0, ACC
 MOV 0, DOWN
 JMP START
ELONE:
 MOV 0, DOWN
 MOV UP, ACC
 JMP START
SUCCESS:
  MOV 2, DOWN
  MOV 1, ACC

@2
START:
JGZ ELONE
 SUB UP
 JLZ SUCCESS
 MOV 0, ACC
 MOV 0, DOWN
 JMP START
ELONE:
 MOV 0, DOWN
 MOV UP, ACC
 JMP START
SUCCESS:
  MOV 3, DOWN
  MOV 1, ACC

@3
START:
JGZ ELONE
 SUB UP
 JLZ SUCCESS
 MOV 0, ACC
 MOV 0, DOWN
 JMP START
ELONE:
 MOV 0, DOWN
 MOV UP, ACC
 JMP START
SUCCESS:
  MOV 4, DOWN
  MOV 1, ACC

@4
MOV UP, RIGHT

@5
START:
 MOV UP, ACC
 JNZ RETURN
 MOV LEFT, DOWN
 JMP START
RETURN:
 MOV ACC, DOWN
 MOV LEFT, ACC

@6
START:
 MOV UP, ACC
 JNZ RETURN
 MOV RIGHT, DOWN
 JMP START
RETURN:
 MOV ACC, DOWN
 MOV RIGHT, ACC

@7
MOV UP, LEFT

@8
MOV UP, RIGHT

@9
START:
 MOV UP, ACC
 JNZ RETURN
 MOV LEFT, DOWN
 JMP START
RETURN:
 MOV ACC, DOWN
 MOV LEFT, ACC

@10
#271/10/65
